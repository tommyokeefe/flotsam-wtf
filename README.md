# Flotsam
Basic Astro blog with Sakura for minimal CSS theming. 

To create a new post add a new `.md` file in `src/pages/posts/`.

To create a new page add a new `.md` file in `src/pages/`. Filename will determine route.

For local dev run:
```
    npm install
    npm run dev
```
